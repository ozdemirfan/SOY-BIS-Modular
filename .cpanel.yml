---
# Runs one script so all steps share the same shell (cPanel may run each task in a new shell).
deployment:
  tasks:
    - /bin/bash /home/u2626680/repositories/SOY-BIS-Modular/scripts/cpanel-deploy.sh
